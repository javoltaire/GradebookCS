<Page
    x:Class="GradebookCS.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GradebookCS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <RelativePanel x:Name="rootPane" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock x:Name="welcomeTextBlock" Text="Welcome, User"/>
        <TextBox x:Name="searchTextBox" Width="250" PlaceholderText="Search" RelativePanel.AlignHorizontalCenterWithPanel="True"/>
        <Button x:Name="searchButton" Content="Go" RelativePanel.RightOf="searchTextBox" RelativePanel.AlignVerticalCenterWith="searchTextBox"/>
        <Button x:Name="settingsButton" Content="Se" RelativePanel.AlignRightWithPanel="True"/>
        <Frame x:Name="contentFrame" RelativePanel.Below="searchTextBox" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True">
            
        </Frame>
    </RelativePanel>
</Page>
