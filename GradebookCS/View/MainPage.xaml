<Page
    x:Class="GradebookCS.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GradebookCS.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:GradebookCS.ViewModel"
    xmlns:views="using:GradebookCS.View"
    mc:Ignorable="d">

    <Page.Resources>
        <viewmodels:MainPageViewModel x:Key="MainPageViewModel"/>
    </Page.Resources>

    <RelativePanel x:Name="rootPane" DataContext="{StaticResource MainPageViewModel}" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button x:Name="settingsButton" RelativePanel.AlignRightWithPanel="True" Height="40" Width="40" Command="{Binding ViewSettingsCommand}">
            <SymbolIcon Symbol="Setting"/>
        </Button>
        <TextBlock x:Name="welcomeTextBlock" Text="Welcome" RelativePanel.AlignVerticalCenterWith="settingsButton"/>
        <Frame x:Name="ContentFrame" SourcePageType="{Binding CurrentPageType, Mode=TwoWay}" RelativePanel.Below="settingsButton" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True"/>
    </RelativePanel>
</Page>
