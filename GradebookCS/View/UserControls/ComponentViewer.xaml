<UserControl
    x:Class="GradebookCS.View.UserControls.ComponentViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GradebookCS.View.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox PlaceholderText="Name" Grid.RowSpan="2" VerticalAlignment="Center"/>
        <TextBox PlaceholderText="Weight" Grid.RowSpan="2" Grid.Column="1" VerticalAlignment="Center"/>
        <TextBlock Text="Name - 0" Grid.RowSpan="2" Grid.ColumnSpan="2" VerticalAlignment="Center"/>
        <Button Grid.Column="2">
            <SymbolIcon Symbol="Clear"/>
        </Button>
        <Button Grid.Column="2" Grid.Row="1">
            <SymbolIcon Symbol="Edit"/>
        </Button>
    </Grid>
</UserControl>
