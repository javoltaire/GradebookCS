<ContentDialog
    x:Class="GradebookCS.View.CourseInfoDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GradebookCS.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Course Info"
    PrimaryButtonText="Save"
    SecondaryButtonText="Cancel">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--Name Block-->
        <TextBlock VerticalAlignment="Center" Text="Name:"/>
        <TextBox Grid.Column="1" Grid.ColumnSpan="3" VerticalAlignment="Center" PlaceholderText="Course Name" Text="{Binding Name, Mode=TwoWay}"/>
        <!--A Block-->
        <TextBlock Grid.Row="1" Text="A:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" PlaceholderText="From" Text="{Binding Grade.ARangeLowEnd, Mode=TwoWay}"/>
        <TextBlock Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Text="-" HorizontalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="3" VerticalAlignment="Center" PlaceholderText="To" Text="{Binding Grade.ARangeHighEnd, Mode=TwoWay}"/>
        <!--B Block-->
        <TextBlock Grid.Row="2" Text="B:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" PlaceholderText="From" Text="{Binding Grade.BRangeLowEnd, Mode=TwoWay}"/>
        <TextBlock Grid.Row="2" Grid.Column="2" VerticalAlignment="Center" Text="-" HorizontalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="3" VerticalAlignment="Center" PlaceholderText="To" Text="{Binding Grade.BRangeHighEnd, Mode=TwoWay}"/>
        <!--C Block-->
        <TextBlock Grid.Row="3" Text="C:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" PlaceholderText="From" Text="{Binding Grade.CRangeLowEnd, Mode=TwoWay}"/>
        <TextBlock Grid.Row="3" Grid.Column="2" VerticalAlignment="Center" Text="-" HorizontalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="3" VerticalAlignment="Center" PlaceholderText="To" Text="{Binding Grade.CRangeHighEnd, Mode=TwoWay}"/>
        <!--NR Block-->
        <TextBlock Grid.Row="4" Text="NR:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1" VerticalAlignment="Center" PlaceholderText="From" Text="{Binding Grade.NRRangeLowEnd, Mode=TwoWay}"/>
        <TextBlock Grid.Row="4" Grid.Column="2" VerticalAlignment="Center" Text="-" HorizontalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="3" VerticalAlignment="Center" PlaceholderText="To" Text="{Binding Grade.NRRangeHighEnd, Mode=TwoWay}"/>
    </Grid>
</ContentDialog>

