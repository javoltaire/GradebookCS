<UserControl
    x:Class="GradebookCS.View.UserControls.ComponentViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GradebookCS.View.UserControls"
    xmlns:converters="using:GradebookCS.View.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="32"
    d:DesignWidth="400">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisConverter"/>
    </UserControl.Resources>
    <Grid Width="300">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="25"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox PlaceholderText="Name" Text="{Binding Component.Name, Mode=TwoWay}"  Grid.RowSpan="2" VerticalAlignment="Center" Visibility="{Binding IsInEditMode, Converter={StaticResource BoolToVisConverter}}"/>
        <TextBox PlaceholderText="Weight" Text="{Binding Component.Weight, Mode=TwoWay}" Grid.RowSpan="2" Grid.Column="1" VerticalAlignment="Center" Visibility="{Binding IsInEditMode, Converter={StaticResource BoolToVisConverter}}"/>
        <TextBlock Text="{Binding Component.NameWeight}" Grid.RowSpan="2" Grid.ColumnSpan="2" VerticalAlignment="Center" Visibility="{Binding IsInEditMode, Converter={StaticResource InverseBoolToVisConverter}}"/>
        <Button Grid.Column="2">
            <SymbolIcon Symbol="Clear"/>
        </Button>
        <Button Grid.Column="2" Grid.Row="1" Visibility="{Binding IsInEditMode, Converter={StaticResource InverseBoolToVisConverter}}" Command="{Binding ShowComponentEditingControlsCommand}">
            <SymbolIcon Symbol="Edit"/>
        </Button>
        <Button Grid.Column="2" Grid.Row="1" Visibility="{Binding IsInEditMode, Converter={StaticResource BoolToVisConverter}}" Command="{Binding HideComponentEditingControlsCommand}">
            <SymbolIcon Symbol="Save"/>
        </Button>
    </Grid>
</UserControl>

